%h2 Medical Aids
%table.dataTable
  - @medical_aids.each do |t|
    %tr
      %td=t.name
      %td=link_to "View", medical_aid_path(t.id)
      %td=link_to "Edit", edit_medical_aid_path(t.id)
      %td=link_to "Delete", medical_aid_path(t.id),confirm: 'Are you sure you want to delete this medical aid?', method: :delete


=link_to "New Medical Aid", new_medical_aid_path