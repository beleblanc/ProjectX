%fieldset.well.form-inline
  =f.input :soap_type_id, :collection =>SoapType.all, :label_method=>:name, :value_method=>:id ,:input_html=>{:class=>"span2"}
  %hr.clearfix
  =f.input :comment,:input_html=>{:class=>"span8",:rows=> 6}
  %hr.clearfix
  =f.fields_for :vitals do |vitals|
    =render 'vital_fields', f: vitals
  =link_to_add_fields "Add Vitals", f, :vitals
